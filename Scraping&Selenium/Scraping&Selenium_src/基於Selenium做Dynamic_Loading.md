## HW要求

目標網站：https://the-internet.herokuapp.com/dynamic_loading

頁面按下 Start 後會顯示 Loading，等待一段時間後才出現結果文字。請分別完成兩個動態載入範例。

程式必須：

- 進入第一個範例。
- 找到並點擊 Start。
- 等待 Loading 消失。
- 等待結果文字顯示。
- 取得結果文字。
- 返回上一頁。
- 操作第二個範例。
- 比較兩個範例的 DOM 差異。
